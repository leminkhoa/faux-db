MyRandomProvider:
  type: "random_choice"
  choices: ["standard", "gold", "platinum", "diamond"]
  seed: 42

FurnitureFullNameWithSeedProvider:
  type: "template_choice"
  templates:
    - "{{ catalog('furniture.material') | cycle }} {{ catalog('furniture.furniture') }}"
    - "test"
  seed: 42
