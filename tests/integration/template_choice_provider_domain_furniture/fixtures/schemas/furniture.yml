furniture:
  rows: 5
  columns:
    label_first:
      type: "$provider"
      target: "FirstFilterProvider"
    label_random:
      type: "$provider"
      target: "RandomFilterProvider"
    label_cycle:
      type: "$provider"
      target: "CycleFilterProvider"
    label_default:
      type: "$provider"
      target: "DefaultFilterProvider"
  output:
    format: csv
    filepath: "./outputs/furniture.csv"
