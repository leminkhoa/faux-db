{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Kuriboh: generate schemas/ecommerce",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "args": ["generate", "schemas/ecommerce"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
