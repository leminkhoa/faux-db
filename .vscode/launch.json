{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Kuriboh: generate schemas/products.yml",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "args": ["generate", "schemas/products.yml"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
